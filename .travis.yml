language: android
jdk:
  - oraclejdk8
android:
  components:
    - tools
    - build-tools-25.0.0
    - android-25
    - extra-android-m2repository

before_script: cp gradle.properties.example gradle.properties
script: ./gradlew testDebug

before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
cache:
  directories:
    - $HOME/.gradle/caches/
    - $HOME/.gradle/wrapper/